# ds_in_da_wild
For ds class project.


In your terminal, direct to the direction that you want to place the project. 
Run ```git clone https://github.com/VincentPit/ds_in_da_wild.git```.
Then, ```git checkout -b <branch-name>```
